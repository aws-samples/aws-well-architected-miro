{
    "name": "miro-app-wat",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "prebuild": "rm -rf exporter/functions",
        "build": "(esbuild functions/**/*.ts --bundle --minify --platform=node --target=es2020 --outdir=exporter/functions --outbase=functions)",
        "predeploy": "(cd frontend && npm install && npm run build) && (cd hosting && npm install) && (cd exporter && npm install)",
        "deploy": "(cd hosting && npm run deploy) && (cd docker && npm run deploy) && (cd exporter && npm run deploy)"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/aws-lambda": "^8.10.109",
        "@types/jsonwebtoken": "^9.0.0",
        "@types/node": "^18.11.18",
        "esbuild": "^0.16.13",
        "prettier": "2.8.1"
    },
    "dependencies": {
        "@aws-sdk/client-ssm": "^3.241.0",
        "@aws-sdk/client-wellarchitected": "^3.241.0",
        "jsonwebtoken": "^9.0.0"
    }
}
