{
  "name": "miro-app-wat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "esbuild functions/*.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outdir=dist",
    "postbuild": "cd dist && for f in *.js; do zip \"${f%.*}.zip\" \"$f\"; done"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^18.11.18",
    "esbuild": "^0.16.13"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.241.0",
    "jsonwebtoken": "^9.0.0"
  }
}
